{
    "game_name": "{{GAME_NAME}}",
    "version": "1.0",
    "description": "Please enter description",
    "min_players": 1,
    "server": {
        "script": "server.py",
        "args_template": "--port {port}"
    },
    "client": {
        "script": "client.py",
        "args_template": "--host {host} --port {port} --user {user}"
    }
}